{
  "name": "pm-energy-calc",
  "devDependencies": {
    "esbuild": "^0.14.38"
  },
  "version": "1.0.0",
  "description": "web calc for energy prices",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild --bundle src/app.js --outfile=public/app.js --target=es6",
    "start": "npm run build -- --servedir=public",
    "watch": "npm run build -- --watch",
    "server": "node server.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DataFlash22/pm-energy-calc.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/DataFlash22/pm-energy-calc/issues"
  },
  "homepage": "https://github.com/DataFlash22/pm-energy-calc#readme",
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
